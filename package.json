{
  "name": "scanner-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/scanner",
    "build": "npm run build --workspace=apps/scanner",
    "lint": "npm run lint --workspace=apps/scanner"
  }
}
